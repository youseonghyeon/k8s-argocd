apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-elk.fullname" . }}
  labels:
    {{- include "my-elk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-elk.selectorLabels" . | nindent 4 }}
