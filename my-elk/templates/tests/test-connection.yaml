apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-elk.fullname" . }}-test-connection"
  labels:
    {{- include "my-elk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-elk.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
